export const metadata = {
  title: 'WhatsApp Lead Bot',
  description: 'Bot de captura y conversión de leads por WhatsApp',
}

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  )
}
